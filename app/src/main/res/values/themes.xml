<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Thème principal : SANS ActionBar -->
    <style name="Theme.ReverseClock"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>

    </style>


    <!-- Fullscreen réel -->
    <style name="Theme.ReverseClock.Fullscreen"
        parent="Theme.ReverseClock">

        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.ReverseClock.FullscreenContainer" parent="">

        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="fullscreenBackgroundColor">@color/black</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>

</resources>
